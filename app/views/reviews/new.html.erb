          <div class="mt-3">

            <%= simple_form_for [@cocktail, @review] do |f| %>
                <%= f.input :name %>
                <%= f.input :rating %>
                <%= f.input :content %>
                <%= f.button :submit, :class => "btn btn-primary" %>
                <% end %>
              </div>
